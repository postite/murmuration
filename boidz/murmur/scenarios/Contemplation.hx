package murmur.scenario;

class Contemplation extends TimedScenario implements IScenario{

	public function new(can:murmur.Canvas,clientId,delay:Int,?maxTime:Int){
		super(can,clientId);
	}
	override function execute(){

	}
}